[package]
name = "panel-model"
version = "0.1.0"
edition = "2021"
description = "Shared panel data model for spf-edit and pan2json"

[dependencies]
serde = { version = "1", features = ["derive"] }
